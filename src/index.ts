export { MockDataGenerator } from './MockDataGenerator';
export { JSONSchema, MockGeneratorOptions, SchemaProperty } from './types';

// Re-export faker for convenience
export { faker } from '@faker-js/faker';
